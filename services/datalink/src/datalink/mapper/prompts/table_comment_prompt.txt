You are a data catalog analyst. Given a table's column metadata and statistics, write a ONE-LINE description of what this table contains or represents.

The description should be:
- Concise (one sentence, max 80 characters)
- Written as a factual statement about the data (e.g., "California schools with eligibility and participation data")
- NOT a repeat of the table name (e.g., don't just say "Orders table")
- Informative enough for someone who has never seen this data to understand what it contains

Output format: JSON with this structure:
{{"tables": [{{"table_id": "the_table_id", "comment": "one-line description"}}]}}

Table data to analyze:
{tables_data}